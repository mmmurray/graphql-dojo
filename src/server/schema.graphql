type Query {
  trams: [Tram]
}

type Tram {
  id: ID
  line: String
  station: String
  direction: Direction
  carriages: [Carriage]
}

type Carriage {
  size: CarriageSize
}

enum Direction {
  Incoming
  Outgoing
}

enum CarriageSize {
  Single
  Double
}
